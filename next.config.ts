import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains: ['i.pinimg.com',"cdn.pixabay.com",'img.icons8.com', 'www.usedash.ai','images.unsplash.com',"cdn.builder.io" ,"bookface-images.s3.us-west-2.amazonaws.com"],
  }
};

export default nextConfig;
